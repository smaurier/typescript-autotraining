// types.ts — STARTER (page blanche : rien n'est fourni, c'est voulu)
// C'est ICI que tu écris la source unique de vérité du domaine TribuZen.
// Exports attendus par l'oracle (test/) via `@lab/types` :
//   MemberRole, InvitationStatus, Family, MemberBase, AdminMember, Member, Post, Invitation
// Cahier des charges exact : README § Énoncé.

export {};
