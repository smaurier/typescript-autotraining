// geo.ts — ÉTAT À CORRIGER (le commit fautif du collègue)
// L'oracle (test/) importe ce fichier via `@lab/geo` et attend : LatLng, distanceKm, itineraire

export function distanceKm(a: number[], b: number[]): number {
  // a[0], a[1] peuvent être n'importe quoi : rien ne garantit [lat, lng]
  // ...
  return 0;
}
